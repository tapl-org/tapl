language pythonlike

from tapl_lang.lib import tapl_dev

def fibonacci(n: Int) -> Int:
    if n <= 1:
        return n
    else:
        return fibonacci(n - 1) + fibonacci(n - 2)

TYPECHECK_ONLY:
    tapl_dev.log(fibonacci)
print(fibonacci(3))
print(fibonacci(4))